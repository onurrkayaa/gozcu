# Gozcu - Hafta 0 taban cizgisi olcumu icin sabitlenmis paket surumleri.
# Surumler sabit; ayni requirements ile ayni sayilar uretilsin diye.
# Kurulum: python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt
ultralytics==8.4.144
sahi==0.12.6
pillow==12.3.0
pandas==3.0.5
tqdm==4.70.0
matplotlib==3.11.1
pytest==9.1.1
# PDF uretimi (06_rapor_uret.py). Saf Python; brew/sistem bagimliligi yok.
reportlab==5.0.1
# ONNX disa aktarim ve PyTorch-ONNX karsilastirmasi (scripts/17, scripts/18).
onnx==1.22.0
onnxslim==0.1.96
onnxruntime==1.30.0
