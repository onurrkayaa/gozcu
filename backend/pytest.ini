[pytest]
DJANGO_SETTINGS_MODULE = gozcu_api.settings
python_files = test_*.py
testpaths = tests
addopts = -q
# Konteyner root olmayan kullaniciyla calisir ve /app ona yazilabilir degil;
# onbellek yazilabilir bir yere alinir.
cache_dir = /tmp/.pytest_cache
